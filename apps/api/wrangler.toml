name = "2990s-api"
main = "src/index.ts"
compatibility_date = "2026-05-08"
compatibility_flags = ["nodejs_compat"]

# Public env (non-secrets). Secrets set via `wrangler secret put`:
#   wrangler secret put SUPABASE_ANON_KEY
#   wrangler secret put SUPABASE_SERVICE_ROLE_KEY
[vars]
SUPABASE_URL = "https://dolvxrchzbnqvahocwsu.supabase.co"

# Allowed CORS origins (dev + prod). Update when domain is set.
ALLOWED_ORIGINS = "http://localhost:5173,http://localhost:5174"

# R2 bindings for slip + receipt storage (Phase 4 wires these up):
# [[r2_buckets]]
# binding = "SLIPS"
# bucket_name = "2990s-slips"

# Cron triggers for the slip-orphan reaper (Phase 4):
# [triggers]
# crons = ["*/10 * * * *"]
